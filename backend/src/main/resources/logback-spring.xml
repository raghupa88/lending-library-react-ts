<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Human-readable console logging for local development -->
    <springProfile name="!docker">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Structured JSON logs (ECS-ish) for containers: one event per line on
         stdout, ready for Filebeat/Kibana in the observability phase. -->
    <springProfile name="docker">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <logger>log.logger</logger>
                    <level>log.level</level>
                    <thread>process.thread.name</thread>
                </fieldNames>
                <customFields>{"service.name":"lending-library-api"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
